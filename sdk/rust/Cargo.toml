[package]
name = "klaxond-protocol"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/luigibarretta/klaxond"
description = "Canonical application-event payloads for Klaxond"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
